import assert from 'node:assert/strict';import fs from 'node:fs';import {login,authenticate,logout,adminSecurityStatus} from '../services/api/src/store/admin-store.mjs';import {databaseMode,databaseHealth} from '../services/api/src/db/postgres.mjs';
const bad=login('admin','wrong','test-client');assert.equal(bad,null);const ok=login('admin',process.env.ADMIN_PASSWORD||'admin123','test-client-ok');assert.ok(ok?.token);assert.equal(authenticate(`Bearer ${ok.token}`).role,'SUPER_ADMIN');assert.equal(logout(`Bearer ${ok.token}`),true);assert.equal(authenticate(`Bearer ${ok.token}`),null);assert.equal(databaseMode(),process.env.DATABASE_URL?'postgres':'memory');if(!process.env.DATABASE_URL){const h=await databaseHealth();assert.equal(h.productionReady,false);}assert.ok(fs.readFileSync(new URL('../services/api/migrations/001_production_foundation.sql',import.meta.url),'utf8').includes('admin_sessions'));assert.ok(adminSecurityStatus().sessionTtlSeconds>0);console.log('v1.1 production foundation test passed');
