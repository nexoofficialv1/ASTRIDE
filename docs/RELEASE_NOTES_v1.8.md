# v1.8 — Admin Control Console

This release replaces the minimal admin shell with a responsive internal operations console. It adds complete navigation for live rides, drivers, payments, settlements, safety/SOS, complaints, notifications, provider switching, service controls, audit and storage status. The console uses the existing secured admin APIs and keeps all customer booking functions in the native mobile apps.
