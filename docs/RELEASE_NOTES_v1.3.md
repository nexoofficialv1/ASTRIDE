# v1.3 Release Notes

This is a production-data-layer milestone, not a signed public mobile release. The API contract remains backward compatible. Real-time components are designed for horizontal scaling with Redis and durable PostgreSQL recovery.
