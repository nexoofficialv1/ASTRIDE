# v3.9.3 Final VPS Bootstrap
- Persistent encrypted provider vault and persistent runtime config.
- Admin credential create/replace/delete UI.
- Production bootstrap no longer requires live providers at process startup.
- Booking and driver-online operations default disabled until Admin enables them.
- Runtime Firebase public client options; APK can be built before Firebase configuration.
- Passenger, Driver and Partner signed release workflow.
- Firebase and native Google Maps build inputs optional.
- Deployment preflight variable names aligned with actual code.
