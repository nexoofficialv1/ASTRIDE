# Sprint 2 Status

## Implemented
Core business rules are now exposed through runnable backend APIs and promoter scope checks.

## Tested
Run `npm run test:sprint2` from repository root. Existing regression suite should also be run with `npm test`.

## Honest limitation
This package is source code, not a compiled APK. Flutter/Android build remains a separate authoritative gate.
